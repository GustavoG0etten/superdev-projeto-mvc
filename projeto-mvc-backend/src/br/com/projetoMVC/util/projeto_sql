-- Declaração de tabelas 

CREATE TABLE produto (
	id 		   SERIAL,
	descricao  VARCHAR(100) NOT NULL,
	CONSTRAINT pk_produto PRIMARY KEY (id)
);

INSERT INTO produto (descricao) 
VALUES ('Notebook'), ('Mouse'), ('Headset');

SELECT * FROM produto